import { calculateArea, getDate } from "./ultils";

const AreaMap = () => {
    printArea = () => {
        const area = calculateArea(24, 54);
        console.log(area);
    }
}
export default AreaMap;
